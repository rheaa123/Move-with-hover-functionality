# Move-with-hover-functionality

- Ignore the commented part in this repo lol just trying different things.
- In this code Hidden and Popup pages are tabs instead of pages. (which we can also try doing instead of pages either work)
- Storage works in Hidden tab not popup one
- move hover function works
